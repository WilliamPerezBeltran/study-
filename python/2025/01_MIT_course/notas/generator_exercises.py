"""
Generador simple
    -Crea una función contador(n) que haga yield de los números de 1 a n.
    -Recorre la función con un for.

Generador de cuadrados
    -Crea cuadrados(n) que devuelva los cuadrados de los números de 1 a n.

Filtro con generador
    - Crea pares(n) que haga yield solo de los números pares hasta n.
"""
